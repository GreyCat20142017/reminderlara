import React from 'react'
import Layout from '@/Shared/Layout'

const Content = ({title}) => {
  return (
    <Layout>
      <h1>{title}</h1>
      <p>Тут будет что-то с чем-то</p>
    </Layout>
  )
};

export default Content;
